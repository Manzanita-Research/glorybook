---
phase: 01-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/client/app.css
  - public/fonts/CommitMono-Variable.woff2
autonomous: true
requirements: []

must_haves:
  truths:
    - Fraunces variable font loads from Google Fonts CDN with WONK and SOFT axes
    - Commit Mono variable font loads from self-hosted woff2 file
    - font-display utility class applies font-variation-settings for WONK 1 and SOFT 100
    - font-mono class applies Commit Mono with OpenType feature settings
    - Film grain CSS class exists and renders SVG noise overlay
    - Selection style uses bark background with cream text
  artifacts:
    - public/fonts/CommitMono-Variable.woff2
    - index.html (with Google Fonts link tags)
    - src/client/app.css (with @font-face, @theme fonts, utility classes, grain, selection)
  key_links:
    - Fraunces loaded via Google Fonts in index.html
    - Commit Mono loaded via @font-face in app.css referencing public/fonts/
    - @theme block defines --font-display and --font-mono custom properties
---

<objective>
Set up font infrastructure and CSS foundation for Manzanita brand typography in Glory.

Purpose: Establish the font loading, Tailwind theme tokens, and CSS utility classes that all subsequent visual polish depends on. This is the foundation layer — fonts must load before components can use them.

Output: Fraunces and Commit Mono fonts available via Tailwind utilities, film grain class ready, selection style applied.
</objective>

<execution_context>
@/Users/jem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@index.html
@src/client/app.css
@.planning/phases/01-visual-polish/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy Commit Mono font and add font loading to index.html</name>
  <files>
    public/fonts/CommitMono-Variable.woff2
    index.html
  </files>
  <action>
1. Create `public/fonts/` directory.
2. Copy the Commit Mono variable font from the brand repo:
   ```bash
   cp /Users/jem/code/manzanita-research/brand/org/fonts/CommitMono-Variable.woff2 public/fonts/
   ```
3. Add Google Fonts preconnect and Fraunces link to `index.html` `<head>`, before `<title>`:
   ```html
   <link rel="preconnect" href="https://fonts.googleapis.com" />
   <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
   <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght,SOFT,WONK@0,9..144,100..900,0..100,0..1;1,9..144,100..900,0..100,0..1&display=swap" rel="stylesheet" />
   ```
   Place after the `apple-mobile-web-app-status-bar-style` meta tag and before `<title>`.
  </action>
  <verify>
    <automated>test -f public/fonts/CommitMono-Variable.woff2 && grep -q "Fraunces" index.html && grep -q "preconnect" index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Commit Mono woff2 exists in public/fonts/. index.html has Google Fonts preconnect + Fraunces link with all variable axes (ital, opsz, wght, SOFT, WONK).</done>
</task>

<task type="auto">
  <name>Task 2: Add @font-face, theme tokens, utility classes, grain, and selection to app.css</name>
  <files>src/client/app.css</files>
  <action>
Add the following to `src/client/app.css`, keeping the existing content intact:

1. **After `@import "tailwindcss";` and before `@custom-variant`**, add the Commit Mono @font-face declaration:
   ```css
   /* Commit Mono — custom Manzanita build */
   @font-face {
     font-family: 'Commit Mono';
     src: url('/fonts/CommitMono-Variable.woff2') format('woff2');
     font-weight: 100 900;
     font-display: swap;
   }
   ```

2. **Inside the existing `@theme` block**, add font family tokens at the top (before color definitions):
   ```css
   /* Brand typography */
   --font-display: 'Fraunces', Georgia, serif;
   --font-mono: 'Commit Mono', monospace;
   ```

3. **After the `.oled` class block**, add these utility classes:
   ```css
   /* Fraunces variable font — soft wonky display */
   .font-display {
     font-variation-settings: 'WONK' 1, 'SOFT' 100;
   }

   /* Commit Mono — Manzanita custom build OpenType features */
   .font-mono {
     font-feature-settings:
       'cv01' 1, 'cv03' 1, 'cv04' 1, 'cv06' 1, 'cv11' 1,
       'ss01' 1, 'ss02' 1, 'ss03' 1, 'ss04' 1, 'ss05' 1;
   }

   /* Brand selection color */
   ::selection {
     background-color: #6b3a2a;
     color: #f5f0e8;
   }

   /* Film grain — scoped overlay for specific sections */
   .grain {
     position: relative;
   }

   .grain::after {
     content: '';
     position: absolute;
     inset: 0;
     opacity: 0.06;
     pointer-events: none;
     mix-blend-mode: soft-light;
     background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
     background-repeat: repeat;
     background-size: 256px 256px;
     z-index: 1;
   }
   ```

**Important:** The `.font-mono` rule applies OpenType features globally to anything using the `font-mono` Tailwind class. The `--font-mono` theme token makes Tailwind's `font-mono` utility resolve to Commit Mono. Together these ensure chord charts automatically get Commit Mono with the right features.
  </action>
  <verify>
    <automated>grep -q "font-face" src/client/app.css && grep -q "font-display" src/client/app.css && grep -q "WONK" src/client/app.css && grep -q "grain" src/client/app.css && grep -q "::selection" src/client/app.css && grep -q "font-feature-settings" src/client/app.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>app.css contains: @font-face for Commit Mono, --font-display and --font-mono in @theme, .font-display class with WONK/SOFT, .font-mono class with OpenType features, ::selection style, .grain class with SVG noise overlay.</done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds (no CSS/font import errors)
- `public/fonts/CommitMono-Variable.woff2` exists and is a valid font file
- `index.html` loads Fraunces from Google Fonts with all variable axes
- `app.css` has all required CSS: @font-face, @theme fonts, utility classes, grain, selection
</verification>

<success_criteria>
- Font infrastructure is in place for components to use `font-display` and `font-mono` classes
- Film grain `.grain` class is ready to be applied to JoinScreen
- No existing functionality broken — all existing classes still work
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-polish/01-01-SUMMARY.md`
</output>
